<div id="user_menu">
  <% if logged_in? %>
    <p>Hello there, <%= current_user.username %>!</p>
    <%= button_to "Sign Out", session_url, method: :delete %>
  <% else %>
    <%= link_to "Sign In", new_session_url %>
    |
    <%= link_to "Sign Up", new_user_url %>
  <% end %>
</div>